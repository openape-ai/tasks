# Prod compose for tasks.openape.ai on chatty — tested image from
# registry.openape.ai, packaged by compose/package.Dockerfile and deployed by
# scripts/deploy-image.mjs (which syncs this file to
# /home/openape/prod-tasks/docker-compose.yml).
#
# Same design as the monorepo's compose/chatty.yml:
# - Mounts the existing systemd-era shared/ at the IDENTICAL path and reuses
#   shared/.env via env_file — the absolute file:-SQLite URL keeps working.
# - HOST is overridden to 0.0.0.0 (the .env pins 127.0.0.1, which would bind
#   loopback INSIDE the container); `environment` wins over `env_file`.
# - Port publishes on 127.0.0.1:3005 — exactly where nginx already proxies,
#   so the nginx config is untouched by the systemd→container cutover.
# - user: 999:988 (openape) so the DB/WAL files stay openape-owned and the
#   dormant systemd fallback could still take over.
# - Image tag pinned via /home/openape/prod-tasks/.env (TASKS_TAG,
#   TASKS_TAG_PREV is the rollback pin).

name: openape-tasks

services:
  tasks:
    image: registry.openape.ai/openape-tasks:${TASKS_TAG:-unset}
    restart: unless-stopped
    user: "999:988"
    env_file: /home/openape/projects/openape-tasks/shared/.env
    environment:
      HOST: 0.0.0.0
      NITRO_HOST: 0.0.0.0
    volumes:
      - /home/openape/projects/openape-tasks/shared:/home/openape/projects/openape-tasks/shared
    ports:
      - "127.0.0.1:3005:3005"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3005/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s
